# Keep TIP-2 API warm during market hours (IST).
#
# GitHub Actions cron is UTC-only. IST = UTC+5:30.
# One schedule: every 3 minutes, Mon–Fri, 03:00–09:59 UTC
#   → 08:30–15:29 IST (≈ 8:30 AM – 3:30 PM IST)
#
# scripts/ping.sh still gates on Asia/Kolkata as a safety net.
# Note: GitHub may delay scheduled runs under load.

name: Keep TIP API warm

on:
  schedule:
    # Mon–Fri every 3 min, 03:00–09:59 UTC = 08:30–15:29 IST
    - cron: "*/3 3-9 * * 1-5"
  workflow_dispatch:
    inputs:
      force:
        description: "Ping even outside 08:30–15:30 IST"
        required: false
        default: "false"
        type: choice
        options:
          - "false"
          - "true"

permissions:
  contents: read

jobs:
  ping:
    name: Ping /api/health
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Ping health (IST window)
        env:
          HEALTH_URL: ${{ vars.HEALTH_URL }}
          FORCE: ${{ inputs.force == 'true' && '1' || '0' }}
        run: |
          chmod +x scripts/ping.sh
          bash scripts/ping.sh
